require './app'
require 'sinatra/activerecord/rake'

# namespace :db do
#   desc "Truncate all tables"
#   task :truncate => :environment do
#     conn = ActiveRecord::Base.connection
#     issues = conn.execute("show tables").map { |r| r[0] }
#     issues.delete "schema_migrations"
#     issues.each { |t| conn.execute("TRUNCATE #{t}") }
#   end
# end